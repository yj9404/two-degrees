# 운영 환경(Vercel, Railway, Render 등)에서는 
# 각 플랫폼의 환경변수 설정 화면에서 아래 URL을 입력해주세요.

# 무료 PostgreSQL 템플릿(Neon, Supabase) 예시 (비밀번호와 호스트명 변경 필요)
DATABASE_URL=postgresql://user:password@hostname/dbname?sslmode=require

# 관리자 비밀번호
ADMIN_PASSWORD=custom_admin_password